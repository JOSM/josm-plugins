<?xml version="1.0" encoding="utf-8"?>
<project name="wikipedia" default="dist2" basedir=".">

    <!-- enter the SVN commit message -->
    <property name="commit.message" value="Commit message"/>

    <!-- edit the properties of this plugin in the file `gradle.properties` -->
    <property file="${basedir}/gradle.properties"/>
    <!-- The description has to stay here AND in the build.gradle, so Ant can sync the description to Launchpad and Gradle can also pick it up. -->
    <property name="plugin.description" value="Simplifies linking OSM objects to Wikipedia articles and Wikidata items"/>

    <!-- ** include targets that all plugins have in common ** -->
    <import file="../build-common.xml"/>

    <target name="dist2" depends="dist">
      <copy file="${plugin.jar}" todir="${plugin.dist.dir}/../.josm/plugins"/>
    </target>

</project>
